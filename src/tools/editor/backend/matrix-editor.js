// Required imports
const path = require('path');

// Function that uses path.join for cross-platform compatibility
const filePath = path.join('src', 'tools', 'editor', 'backend', 'matrix-editor.js');

// Other code in matrix-editor.js...